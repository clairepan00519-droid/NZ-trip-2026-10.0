# 家人同步 v13：舊圖片搬移修正版

1. 先在 Supabase SQL Editor 執行 `SUPABASE_SETUP.sql`。
2. 將本資料夾全部檔案覆蓋 GitHub 儲存庫根目錄。
3. 首次請在「仍保有舊圖片的原裝置」開啟網站並保持網路連線，不要清除網站資料。
4. 頁首會顯示搬移進度。完成後改為「家人共享已同步」。

本版會同時清理本機與 Supabase `nz_sync` 中殘留的 Base64 圖片，將它們上傳到 `trip-media` Storage，再把同步資料改成短網址。本機快取容量不足時只略過本機快取，不會中止雲端同步。
